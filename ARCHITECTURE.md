# 饮品托盘消除小游戏架构

## 目标

还原参考视频中的竖屏轻量消除玩法：玩家点击棋盘上未被遮挡的托盘，托盘进入下方备餐区；同类饮品自动归并，单盘集满 6 杯后完成交付、获得金币并推进关卡。备餐位被占满且无法继续归并时失败。

## 技术选型

- React 18 + TypeScript + Vite：组件化、热更新快，适合移动端网页原型。
- DOM + CSS 动画：棋盘元素数量少，交互以点击和位移动画为主，不需要 Canvas；同时便于响应式布局、可访问性和后续换素材。
- `useReducer` 驱动的纯状态机：所有规则通过明确 action 推进，避免动画/UI 状态污染游戏规则。
- Vitest：覆盖归并、消除、胜负与重开等核心规则。

## 分层

1. `src/game/types.ts`
   定义饮品、托盘、棋盘格、顾客订单、关卡和游戏状态。
2. `src/game/levels.ts`
   保存可复现的关卡数据与初始堆叠关系；视觉层不直接构造关卡。
3. `src/game/engine.ts`
   纯函数状态机：选择托盘、检测遮挡、归并饮品、完成订单、推进进度、判定胜负、使用道具。
4. `src/Prototype.tsx`
   页面编排与交互入口，向状态机派发 action。
5. `src/components/*`
   顾客区、订单栏、棋盘、备餐台、底部道具和结果弹窗。
6. `src/prototype.css`
   复刻参考视频的暖色甜品店风格、竖屏布局与反馈动画。

## 核心数据模型

- `Tray`: 六个槽位的饮品数组、棋盘坐标、堆叠层级和可选状态。
- `BoardSlot`: 同一位置可堆叠多个托盘；只有没有更高层托盘覆盖时才能选择。
- `StagingTray`: 备餐台上的托盘；新托盘进入后按饮品类型稳定归并。
- `CustomerOrder`: 当前顾客需要的饮品类型与奖励。
- `GameState`: 金币、关卡进度、棋盘托盘、备餐位、订单、连击、道具次数和阶段。

## 一次操作的状态流

1. 校验所点托盘是否仍在棋盘且没有被覆盖。
2. 若备餐台有空位，将托盘移动到最左侧空位。
3. 按饮品类型将备餐台内容稳定归并，每盘容量为 6。
4. 任一托盘达到 6 个同类饮品时，播放完成状态并从备餐区移除。
5. 若类型命中当前订单，则结算金币、推进进度并刷新顾客；否则只计普通消除。
6. 清空所有棋盘与备餐饮品则胜利；备餐位满且没有可完成组合则失败。

## 约束与扩展点

- 首版按用户许可使用 emoji/文字占位素材，所有素材映射集中在饮品/顾客配置中，方便整体替换。
- 关卡数据与引擎分离，可新增多关、随机种子和难度曲线。
- 道具通过 action 实现，不直接修改 DOM：重新发放、移除一个、消除一列。
- 视觉动画只响应状态变化；动画结束不参与规则计算，避免快速点击造成状态错乱。

